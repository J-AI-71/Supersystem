# Beitrag leisten

- Fehler melden: `tools.html` nutzen, Beispiel-URL + erwartetes Ergebnis anhängen.
- Neue Regeln: Domain, Beispiel-URL, gewünschte Behandlung.
- PRs: bitte nur `app.html`/`app-classic.html` nicht splitten, da GitHub Pages ohne Build läuft.
- Style: Vanilla JS, keine Abhängigkeiten.