/* /Supersystem/js/page-quickstart.js (v35) */
(() => {
  'use strict';
  const btn = document.getElementById('do-print');
  if (btn) btn.addEventListener('click', () => window.print());
  console.log('[SafeShare] quickstart ready');
})();
